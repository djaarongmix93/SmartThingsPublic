# Games

This directory contains simple command-line games. Currently included:

- **tetris.py** – a minimal Tetris-like game using the `curses` library. Use WASD or arrow keys to move, rotate, or drop pieces, and press `q` to quit.
